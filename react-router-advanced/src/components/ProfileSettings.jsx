// src/components/ProfileSettings.jsx

import React from 'react';

const ProfileSettings = () => (
  <div>
    <h3>⚙️ Profile Settings</h3>
    <p>This is where users can update their preferences and password.</p>
  </div>
);

export default ProfileSettings;